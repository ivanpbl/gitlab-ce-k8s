apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: gitlab-ce
  name: gitlab-ce-var-log-claim
  namespace: ci-cd
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
status: {}